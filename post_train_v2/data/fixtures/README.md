# Data Fixtures

This directory contains small tracked V2-only inputs for local and remote
smoke tests. `teacher_smoke_candidates.jsonl` is canonical normalized
Countdown data and must never be used as a production training dataset.
