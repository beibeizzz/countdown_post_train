# V2 Data Workspace

`fixtures/` contains tracked smoke inputs. `processed/`,
`teacher_rollouts/`, and evaluation outputs are generated locally and are
governed by their Manifest V2 completion markers. Generated training data is
not source code and may be excluded from Git according to repository policy.
