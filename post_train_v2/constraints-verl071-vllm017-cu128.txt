# CUDA runtime baseline
torch==2.10.0
torchvision==0.25.0
torchaudio==2.10.0
vllm==0.17.0

# Trainer and adapter stack
transformers==4.57.6
trl==0.27.0
peft==0.19.1
accelerate==1.13.0
datasets==4.8.5
tokenizers==0.22.2
safetensors==0.7.0

# verl support stack
verl==0.7.1
numpy==1.26.4
ray==2.48.0
tensordict==0.10.0
pyarrow==19.0.1
hydra-core==1.3.2
torchdata==0.11.0

# Text-only workaround: newer OpenCV metadata requires NumPy 2, while
# verl 0.7.1 requires NumPy <2. OpenCV is not used by Countdown.
opencv-python-headless==4.11.0.86

# Install separately after Torch and vLLM:
# flash-attn==2.8.3

