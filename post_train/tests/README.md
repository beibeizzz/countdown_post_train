# Tests

Pure Python tests for shared logic and script helpers.

Run:

```bash
python -m pytest post_train/tests -q
```

GPU, vLLM, and full training smoke runs are intentionally left for the target Ubuntu environment.
