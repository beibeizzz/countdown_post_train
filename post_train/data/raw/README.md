# Raw Data

Optional staging area for raw copies.

The current configs read the original raw files from the workspace-level `datasets/` directory:

- `datasets/raw_train.parquet`
- `datasets/raw_test.json`

Keep this directory for local copies or future dataset snapshots.
